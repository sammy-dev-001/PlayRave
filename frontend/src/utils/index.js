// Animation and Responsive Utilities Index
// Export all animation and responsive utilities for easy import

// Responsive utilities
export { default as ResponsiveUtils, useDimensions } from './ResponsiveUtils';

// Animation utilities (hooks)
export {
    AnimationConfig,
    useFadeIn,
    useSlideIn,
    useScaleAnimation,
    usePulse,
    useShake,
    useGlow,
    useStaggeredAnimation,
    useCountdown,
    useProgress,
    useFloat,
    useRotate,
} from './AnimationUtils';
